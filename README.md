# Salary Prediction App

End-to-end ML pipeline assignment:
- train a salary prediction model
- expose predictions through FastAPI
- analyze outputs with a local LLM
- store results in Supabase
- visualize results in Streamlit

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt